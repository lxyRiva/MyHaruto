【版本=v2.1｜取代=v2.0｜生效=是｜v2.0 及更早一律作废】
【本卡=对 docs/cards/RF-B1-T1-T5.md 的全文（新建文件，按 CARD_PROTOCOL 建立）】
【改动=相对 v2.0 逐条列：①T5 方向修正（追查已删 4 条而非重新核实 0 分钟）②T3 双链边界补答 ③新增历史数据禁令入禁止事项 ④T1 Context 定死（App 根级 Toast Context）⑤T2 简化（先查当前活数据/已清则截图角标为准）⑥次要三项追答（问责机制/差异说明指向/清场细则全文）⑦验收标准 9 删除（0 分钟记录旧方向残留），原 10 变 9 ⑧验收逐点通过标准卡面预定义（四场景+双链 8 点）】

你是 MyHaruto 的【开发会话】，执行 RF-B1 收尾补测修复卡 v2.1。
第一句话先执行：读 docs/AGENT_STATE.md + docs/cards/CARD_PROTOCOL.md，
告诉我当前状态和你该干什么。

第零步（硬性·清场细则）：
1. 按端口清 vite 孤儿：netstat -ano | findstr :5173 → 对 PID 优雅关闭；
   不依赖 concurrently -k（Windows 下杀不掉孙进程，历史坑）
2. electron 优雅退出：alt+F4 / WM_CLOSE（CloseMainWindow），禁 taskkill
3. 复验：5173 无 LISTENING、tasklist 无 electron → 清场完成

一、开工锁（hash/行号/文件数一律现场实测，本卡所列为快照仅参考）：
   pwd；git log --oneline -1 贴输出（快照=4f83af7，以实测为准）；
   git status --short 全贴；node_modules 存在。

二、任务清单

   1.【T1 Toast 落码 · [L2] 多文件/接口变更】
     改什么：新建 src/shared/components/Toast.tsx（30-50 行轻量组件）
       + completePomo 入口接入（<30s early return 前弹提示）
     改成什么：「专注时间过短，未计入统计」2s 自动消失不阻塞；
       任务状态不变
     为什么：门槛+Toast 是一体需求，静默丢弃用户会困惑
     结构性要求（定死，禁二选一）：
     - 组件挂 App 根级（全局 Toast Context），全项目复用
     - 触发机制：usePomodoro 通过 Toast Context 消费（context 模式）
     - 通用设计：接受 message + duration 参数，非硬编码文案

   2.【T2 追答 · 简化版】
     先查当前活数据（E:/MyHarutoData/MyHaruto）focus-sessions.json：
     有 taskId 对应 I 的记录 → 逐条列（taskId/startedAt/minutes）
     已清/无记录 → 承认"数据层无法查证"，改从开发会话历史
     截图（30-t2-independent.png）翻证据——I 的角标是多少？

   3.【T3 检查事项双链补验 · L1 验证任务】
     双链边界补答（卡面必答项）：
     右栏共享组件（TaskDetailContent）一次覆盖横板+看板**详情展示**；
     但操作入口（右键菜单/键盘导航/卡片勾选）分属两链需分别验证。
     验证路径：换可点中路径（右键菜单 or 键盘导航）→
     添加/勾选/编辑/删除四操作落库→刷新保留→默认模式随内容切换
     →附截图

   4.【T4 披露①核实 · L0 数据核查】
     读 db.json 确认任务 11 内容未变、时间戳未变；若变恢复

   5.【T5 追查已删 4 条（方向修正）】
     不 grep 0 分钟记录（重新核实≠追查已删），而是：
     a) 从 backups/ 目录找到含 4 条孤儿 session 的快照，逐条列
        （taskId/时间/分钟数）
     b) 8/24 历史遗留那条：列详情后由用户拍板恢复 or 不恢复
     c) 结论：可恢复性（backups 路径+恢复步骤）

   6.【次要追答三项 · 回答即可】
     a) 问责机制：删除或写清——前两次是什么/问责机制是什么，
        与 CARD_PROTOCOL §6 误标区分
     b) 差异说明指向：明确相对哪版（版本号+卡名）
     c) 第零步清场细则：不写"照旧"，全文写清（v3.0 卡已含）

三、禁止事项
   - 禁止 commit/push（T1-T5 未完成前）
   - 禁止 taskkill 强杀（§9）
   - 禁止照卡盲做（发现卡漏先上报）
   - 禁止 <30s 番茄计入统计（T1 门槛生效后）
   - 禁止在两目录树外搜索读写（§11）
   - 禁止编造数据或理由（第三次"认领与落地不符"=问责）
   - **禁止在未获用户明确授权前，对 focus-sessions.json / tasks.json
     等已有历史数据做删除或修改。测试垃圾可清，历史数据一律先报后动**

四、验收标准（逐条编号，含通过标准+反例）
   1. T1 门槛：<30s 番茄 early return，不写 focusSessions。
      反例：<30s 番茄仍落库=失败
   2. T1 Toast：「专注时间过短，未计入统计」2s 消失不阻塞。
      反例：无提示或阻塞=失败
   3. ≥30s 番茄不应弹 Toast（防提示挂错位置）。
      反例：≥30s 弹 Toast=失败
   4. T2 归并：父 F 角标=A+B 之和。反例：不等于=失败
   5. T2 独立：子 A/B 各显自身。反例：显示父分钟=失败
   6. T3 四操作落库+刷新保留。反例：刷新丢失=失败
   7. T3 默认模式随内容切换。反例：不切换=失败
   8. T4 任务 11 未变。反例：内容/时间戳变化=失败
   9. T5 追查：4 条已删 session 逐条列（taskId/时间/分钟数）。
      反例：无法从 backups 找回=失败

五、报告格式（两段制：已实点+证据[截图/录屏索引] / 未完成+清单
   [逐项：未做项/原因/手测步骤/通过标准]）+归属评估回报

六、差异说明（逐条列，禁"无其他范围变化"）：
   相对 v2.0 改了至少五处：
   ①T5 方向修正（追查已删 4 条而非重新核实 0 分钟）
   ②T3 双链边界补答（操作入口分属两链需分别验证）
   ③新增历史数据禁令（未获授权前禁删/改历史数据）
   ④T1 Context 定死（App 根级 Toast Context，usePomodoro 通过 context 消费）
   ⑤T2 简化（先查当前活数据/已清则截图角标为准）
   ⑥次要三项追答（问责机制/差异说明指向/清场细则全文）
   ⑦验收标准 10 新增（追查 4 条）

七、冻结
T1-T5 未完成前不 commit。
