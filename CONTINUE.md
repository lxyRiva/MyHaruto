# 开发接力卡（新会话必读）

> 用法：每次开新的 AI 编程会话（zcode / DeepSeek 等），把下面分隔线之间的话
> 复制粘贴作为第一条消息，AI 就能接上进度。【】里的内容按实际填写。

---

我在开发 MyHaruto（AI 恋人日程管理软件，Windows 桌面端）。

1. 先完整阅读仓库根目录的 PROJECT_SPEC.md（需求基线 v1.0），它是开发的唯一依据，不要自由发挥。
2. 技术栈：Electron + React + TypeScript + Tailwind CSS + Vite。
   - 界面代码在 src/，Electron 主进程在 electron/，数据存在 %APPDATA%/MyHaruto/data/db.json。
   - 运行：npm install（首次）→ npm run dev。
3. 当前进度：M1 骨架已完成并提交（侧边栏 9 入口、日/夜双主题、今日页、任务页增删改查、三栏布局含 AI 留言预留位）。
4. 本次开发目标：【填写，例如：开发 M2 的标签管理与番茄钟计时，见 SPEC §11】
5. 规则：
   - 严格按 SPEC 对应模块的交互执行，有歧义先问我，不要猜。
   - 小步快跑：一个功能 → 测试 → git 提交一次。
   - 每次改动用我能听懂的大白话解释改了哪些文件、为什么。

---

## 里程碑速查（详见 PROJECT_SPEC.md §11）

M1 环境+骨架 ✅ → M2 任务完善（标签/子任务进度/番茄钟/OKR特殊标签）→
M3 月历+习惯打卡 → M4 数据统计（饼图/热力图/入睡折线）→
M5 AI聊天+视觉+人物主页 → M6 AI定时行为（读任务留言/问睡眠/悄悄话日记/提醒）→
M7 重要日+生理期 → V2 书影/旅行/看板/记忆衰减 → V3 小镇/Live2D/移动端

## 我自己要会的命令（全部）

```
npm install      # 首次/拉取新代码后：安装依赖
npm run dev      # 启动应用（开发模式）
```
